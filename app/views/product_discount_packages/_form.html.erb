<%= form_with(model: product_discount_package) do |form| %>
  <% if product_discount_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_discount_package.errors.count, "error") %> prohibited this product_discount_package from being saved:</h2>

      <ul>
        <% product_discount_package.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :discount_percentage %>
    <%= form.text_field :discount_percentage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
